import { StubImportClient } from "./import-client";

export const metadata = { title: "Bulk Import Stub Companies — Admin" };

export default function BulkImportStubsPage() {
  return <StubImportClient />;
}
